#
# Foobar
/* Foo
bar */
/* @(foo) */
# @(foo)
# foo /* bar */ baz

----------------------------------------------------

[
	["comment", "#"],
	["comment", "# Foobar"],
	["multiline-comment", "/* Foo\nbar */"],
	["multiline-comment", "/* @(foo) */"],
	["comment", "# @(foo)"],
	["comment", "# foo /* bar */ baz"]
]

----------------------------------------------------

Checks for comments.